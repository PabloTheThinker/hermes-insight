[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "hermes-insight"
version = "0.9.2"
description = "Superior pattern-processing harness for AI agents — neurodivergent-inspired connecting-the-dots cognition as software."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
  { name = "Pablo Navarro", email = "PabloTheThinker@users.noreply.github.com" },
]
keywords = [
  "pattern-recognition",
  "ai-agents",
  "hermes-agent",
  "cognition",
  "neurodivergent",
  "spp",
  "agent-harness",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-cov>=5.0"]

[project.scripts]
hermes-insight = "hermes_insight.cli:main"
hinsight = "hermes_insight.cli:main"

[project.urls]
Homepage = "https://github.com/PabloTheThinker/hermes-insight"
Documentation = "https://github.com/PabloTheThinker/hermes-insight#readme"
Repository = "https://github.com/PabloTheThinker/hermes-insight"
Issues = "https://github.com/PabloTheThinker/hermes-insight/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/hermes_insight"]

[tool.hatch.build.targets.sdist]
include = [
  "src/hermes_insight",
  "tests",
  "docs",
  "examples",
  "skills",
  "hermes_plugin",
  "scripts",
  "README.md",
  "LICENSE",
  "pyproject.toml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
